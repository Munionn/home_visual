use bevy::prelude::*;

pub struct DevicesPlugin;

impl Plugin for DevicesPlugin {
    fn build(&self, _app: &mut App) {
        // TODO: Implement device visualization on floorplan (e.g. glows, states)
    }
}
